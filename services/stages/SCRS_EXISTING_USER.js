/* eslint-disable no-template-curly-in-string */

const SCRS_EXISTING_USER = (lang, tokens) => [
  {
    component: 'BrowserTitle',
    props: {
      title: tokens('SCRS.accessYourAccount')
    }
  }
]

export default SCRS_EXISTING_USER
